# MonitorOrEnergyLabel
Accepts an image url and returns whether that's monitor or energy label.
Note: This function is not always correct, this is due to the tesseract library which doesn't read some images!
